type: object
required:
  - email
  - password
properties:
  email:
    description: "User's email address"
    type: string
    format: email
    example: "alexboo@ukr.net"
  password:
    type: string
    description: "User's password"
